apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: site-vitrine-loutik
  namespace: site-internet
spec:
  entryPoints:
    - web
  routes:
    - match: host(`www.loutik.lab`)
      kind: Rule
      services:
        - name: site-vitrine-loutik
          port: 80